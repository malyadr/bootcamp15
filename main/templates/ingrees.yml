apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myingress
  annotations:
      nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx 
  rules:
  - host: a43d5a90f9a094b7d966ead0de7fb2cf-161477199.ap-south-1.elb.amazonaws.com 
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ .Release.Name }}-frontend
            port:
              number: 3000
      # - path: /api/
      #   pathType: Prefix
      #   backend:
      #     service:
      #       name: {{ .Release.Name }}-bc
      #       port:
      #         number: 8080